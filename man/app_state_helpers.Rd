% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxi_appState.R
\name{app_state_helpers}
\alias{app_state_helpers}
\alias{new_app_state}
\alias{dataset_state}
\alias{dataset_fingerprint}
\alias{normalize_selection}
\alias{data_table_widget_state}
\alias{.sanitize_widget_state}
\alias{migrate_app_state}
\alias{validate_app_state}
\alias{sanitize_snapshot_name}
\alias{snapshot_file_name}
\alias{build_app_state}
\alias{write_app_state}
\alias{restore_table_page_length}
\title{Internal state-format constants and helpers}
\usage{
new_app_state(
  dataset_id = NA_character_,
  dataset = NULL,
  selection = list(features = character(), samples = character()),
  app = list(),
  data_space = list(),
  result_space = list(),
  label = NA_character_,
  package_version = as.character(utils::packageVersion("omicsViewer")),
  schema_version = APP_STATE_SCHEMA_VERSION,
  created_at = format(Sys.time(), "\%Y-\%m-\%dT\%H:\%M:\%S\%z")
)

dataset_state(dataset, id = NA_character_)

dataset_fingerprint(dataset, id = NULL)

normalize_selection(selection, allow_na = FALSE)

data_table_widget_state(state)

.sanitize_widget_state(x)

migrate_app_state(state)

validate_app_state(state, dataset = NULL, dataset_id = NA_character_)

sanitize_snapshot_name(name, fallback = "snapshot")

snapshot_file_name(name, dataset_id = "ESVObj.RDS", fallback = "snapshot")

build_app_state(
  dataset,
  dataset_id = NA_character_,
  data_status = list(),
  result_status = list(),
  selected_features = character(),
  selected_samples = character(),
  label = NA_character_,
  package_version = as.character(utils::packageVersion("omicsViewer"))
)

write_app_state(state, path)

restore_table_page_length(length, pageLength)
}
\arguments{
\item{dataset_id}{Active dataset identifier.}

\item{dataset}{Active dataset.}

\item{selection}{List potentially containing `features` and `samples`.}

\item{app}{List with app-level active-tab metadata.}

\item{data_space}{Data-space panel state.}

\item{result_space}{Result-space panel state.}

\item{label}{Snapshot label/name.}

\item{package_version}{Package version to store in the snapshot.}

\item{schema_version}{Integer state schema version.}

\item{created_at}{Snapshot creation timestamp.}

\item{id}{Character dataset identifier.}

\item{allow_na}{Logical; keep NA for legacy states when true.}

\item{state}{State object.}

\item{x}{Module state list.}

\item{name}{Character user input.}

\item{fallback}{Name used when input is empty.}

\item{data_status}{State returned by the data-space module.}

\item{result_status}{State returned by the result-space module.}

\item{selected_features}{Active semantic feature IDs.}

\item{selected_samples}{Active semantic sample IDs.}

\item{path}{Destination file path.}

\item{length}{Saved DataTable page length.}

\item{pageLength}{Module default when no valid saved value exists.}
}
\value{
Internal helpers return state objects, fingerprints, file names or
  (invisibly) side-effect results.
}
\description{
These helpers implement the versioned widget-state object used by omicsViewer
snapshots. The object is deliberately JSON-like: it contains widget settings
and stable identifiers, but no htmlwidgets, analysis results, browser-local
storage, session IDs or absolute temporary paths.

The fingerprint combines stable object structure and identifiers. It is not a
hash of expression values; expression revisions that do not alter IDs or
annotation columns are intentionally not treated as incompatible.

Keep only the page position, page size, ordering, and column filters supplied
by DataTable's `input$<id>_state`. Callers add row selections separately using
stable row identifiers where available.

Snapshot state is restricted to widget settings and semantic row identifiers.
Legacy snapshots can contain a few computed values; those fields are dropped so
downstream plots and analyses are recalculated after restoration.

Legacy snapshots are plain lists whose names start with `eset_` or
`analyst_`, plus `active_feature`/`active_sample`. Migration only namespaces
those fields; child-module field names are left untouched.

Validation warns on incompatible metadata rather than blocking restoration.
Individual modules remain responsible for validating their own choices.

The returned name contains only portable filename characters and cannot
represent a path or parent-directory reference.

This pure helper is separated from the Shiny server so schema construction
can be tested without launching the full application.

The object is first written to a temporary file in the destination directory
and then renamed. A failed write therefore never replaces a valid snapshot.
}
\keyword{internal}
