% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_aiAssistant.R
\name{aiAssistantModule}
\alias{aiAssistantModule}
\alias{ai_assistant_ui}
\alias{ai_assistant_module}
\title{Optional ellmer-backed AI assistant interface}
\usage{
ai_assistant_ui(id)

ai_assistant_module(
  id,
  state,
  state_available,
  feature_data,
  sample_data,
  expression_data,
  selected_features,
  selected_samples,
  apply_state,
  apply_scatter_view,
  store = NULL
)
}
\arguments{
\item{id}{Module ID.}

\item{state}{Builder function called as \code{state(sections)} returning
the compact application state for the requested sections (WP1
progressive disclosure; it isolates its own reactive reads, so it is
safe to call from ellmer tool contexts).}

\item{state_available}{Reactive logical indicating whether dataset state is ready.}

\item{feature_data}{Reactive feature metadata.}

\item{sample_data}{Reactive sample metadata.}

\item{expression_data}{Reactive expression matrix.}

\item{selected_features}{Reactive semantic feature IDs.}

\item{selected_samples}{Reactive semantic sample IDs.}

\item{apply_state}{Callback that validates and applies a proposed semantic
application-state update.}

\item{apply_scatter_view}{Callback that validates and applies a proposed
feature/sample scatter-axis update.}

\item{store}{Canonical widget store (\code{\link{widget_store_new}})
shared with the app modules. When given, the generic widget tier
(\code{list_widgets}, \code{get_widget}, \code{set_widgets}) is
registered alongside the curated tools (plan section 6.3, S3).}
}
\value{
The UI returns Shiny tags. The server module is invoked for its side
  effects and returns NULL invisibly.
}
\description{
The assistant is a session-local, floating chat drawer with constrained declarative figure rendering. It communicates with
the application through compact state snapshots and narrowly typed tools. Those tools never
receive credentials, arbitrary R code, or the complete expression matrix. Package dependencies are intentionally optional so ordinary
omicsViewer use does not require an LLM provider.
}
\keyword{internal}
