% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxi_agentAssistant.R
\name{agentAssistantHelpers}
\alias{agentAssistantHelpers}
\alias{agent_request_limit}
\alias{agent_environment_config}
\alias{agent_validate_provider_config}
\alias{agent_quick_view_records}
\alias{agent_compact_state}
\alias{agent_annotation_catalog}
\alias{agent_search_annotations}
\alias{agent_summarize_annotation}
\alias{agent_normalize_state_update}
\alias{agent_normalize_scatter_view}
\title{Internal AI-assistant state and validation helpers}
\usage{
agent_request_limit()

agent_environment_config()

agent_validate_provider_config(
  provider,
  model = "",
  api_key = "",
  base_url = ""
)

agent_quick_view_records(views)

agent_compact_state(
  state,
  annotations = NULL,
  quick_views = NULL,
  available_tabs = NULL,
  figure_grammar = NULL
)

agent_annotation_catalog(feature_data, sample_data)

agent_search_annotations(
  space,
  query,
  feature_data,
  sample_data,
  max_results = 20L
)

agent_summarize_annotation(
  space,
  column,
  feature_data,
  sample_data,
  max_values = 12L
)

agent_normalize_state_update(
  update,
  data_tabs,
  analysis_tabs,
  feature_ids,
  sample_ids,
  max_features = 5000L,
  max_samples = 2000L
)

agent_normalize_scatter_view(
  space,
  quick_view_id = NULL,
  x_axis = NULL,
  y_axis = NULL,
  quick_views = NULL,
  feature_columns = character(),
  sample_columns = character()
)
}
\arguments{
\item{provider}{Single provider name: openai or anthropic.}

\item{model}{Optional model name. An empty value uses the provider default.}

\item{api_key}{Optional session API key.}

\item{base_url}{Optional HTTPS or local HTTP API endpoint.}

\item{views}{A data.frame from \code{\link{prepare_quick_views}}, or NULL.}

\item{state}{Versioned application state from \code{\link{build_app_state}}.}

\item{annotations}{Annotation catalog created by
\code{\link{agent_annotation_catalog}}.}

\item{quick_views}{Named list of feature/sample quick-view data.frames.}

\item{available_tabs}{Named list of valid data-space and analysis-space tabs.}

\item{feature_data}{Feature metadata.}

\item{sample_data}{Sample metadata.}

\item{space}{Feature or sample scatter space.}

\item{query}{Case-insensitive query. Matching is literal, not regular-expression.}

\item{max_results}{Maximum number of row IDs to return.}

\item{column}{Exact annotation column name.}

\item{max_values}{Maximum number of categorical values to return.}

\item{update}{Named list with optional \code{data_space_tab},
\code{analysis_space_tab}, \code{features}, and \code{samples}.}

\item{data_tabs}{Allowed data-space tab labels.}

\item{analysis_tabs}{Allowed analysis-space tab labels.}

\item{feature_ids}{Valid feature IDs.}

\item{sample_ids}{Valid sample IDs.}

\item{max_features}{Maximum number of selected features.}

\item{max_samples}{Maximum number of selected samples.}

\item{quick_view_id}{Optional quick-view ID.}

\item{x_axis}{Optional exact annotation column for the X axis.}

\item{y_axis}{Optional exact annotation column for the Y axis.}

\item{feature_columns}{Valid feature annotation columns.}

\item{sample_columns}{Valid sample annotation columns.}
}
\value{
Provider configuration, compact state, annotation summaries, or a
  validated assistant update.

Integer request limit from 1 through 200. Default: 40.

A list with provider, model, base URL, API key, configuration
  status, and credential source. An empty API key means that the user must
  supply one in the current session.

A normalized provider-configuration list without changing the
  credential source.

A list of JSON-like quick-view records.

A JSON-like list containing identifiers, active tabs, semantic
  selections, widget state, annotation metadata, and bounded quick-view
  definitions. Expression values, fingerprints, credentials, and timestamps
  are intentionally omitted.

A named list containing dimensions, row ID labels, and column-level
  type/missingness/cardinality summaries.

A JSON-like search result bounded by \code{max_results}. It never
  returns an entire annotation table or expression matrix.

A bounded numeric or categorical summary. Raw row-level values are
  not returned.

A list containing only explicitly requested, validated changes. An
  empty character vector explicitly clears a selection.

A validated scatter-space and axis update.
}
\description{
These helpers provide the compact, JSON-like state bridge between the Shiny
application and an optional ellmer-powered assistant. They deliberately
return bounded metadata and summaries rather than expression matrices or API
credentials.

The limit applies to all provider requests in one Shiny session, including
intermediate requests generated while a model resolves tool calls.

Environment variables are intentionally limited to provider selection, model
selection, an optional OpenAI-compatible base URL, and credentials. The API
key is never placed in Shiny UI state, snapshots, tool results, or logs.
}
\keyword{internal}
