% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxi_appLog.R
\name{appConsoleLogHelpers}
\alias{appConsoleLogHelpers}
\alias{applog_dir}
\alias{applog_new_file}
\alias{applog_begin}
\alias{applog_end}
\alias{applog_write}
\title{omicsViewer app console-log helpers (internal)}
\usage{
applog_dir()

applog_new_file(dir = applog_dir())

applog_begin(log_file = NULL)

applog_end(env)

applog_write(env, level, condition)
}
\arguments{
\item{log_file}{\code{NULL} (default: auto-named file in
\code{\link{applog_dir}}), an explicit file path, or \code{FALSE} to
disable logging.}

\item{env}{logger environment returned by \code{applog_begin}
(\code{NULL} is allowed and a no-op).}

\item{level}{single character condition level (e.g. "MESSAGE")}

\item{condition}{a condition caught by a calling handler}
}
\value{
\code{applog_dir} returns the log directory in effect.

\code{applog_new_file} creates the log directory if needed and
  returns the path of a fresh per-run log file.

\code{applog_begin} opens the log file, installs the output
  sink (split console echo) and writes a header. It returns an
  environment with \code{$path}, \code{$con} and a \code{$restore()}
  closure, or \code{NULL} when logging is disabled.

\code{applog_end} restores the console and closes the file;
  \code{applog_write} appends one timestamped condition entry.
}
\description{
Resolve the log destination, start/stop the console tee, and write
timestamped condition entries. Not exported; used by
\code{\link{omicsViewer}}.
}
\keyword{internal}
