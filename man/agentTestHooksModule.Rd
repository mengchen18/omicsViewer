% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_agentTestHooks.R
\name{agentTestHooksModule}
\alias{agentTestHooksModule}
\alias{agent_test_hooks_enabled}
\alias{agent_test_hooks_ui}
\alias{agent_test_hooks_module}
\title{Internal environment-gated agent bridge test hooks}
\usage{
agent_test_hooks_enabled()

agent_test_hooks_ui(id)

agent_test_hooks_module(
  id,
  apply_state,
  apply_scatter_view,
  state,
  store = NULL,
  apply_enrichment = NULL,
  apply_table_view = NULL,
  assistant = NULL
)
}
\arguments{
\item{id}{Module ID.}

\item{apply_state}{Callback applying a validated assistant state update
(as used by the \code{set_omics_viewer_state} tool).}

\item{apply_scatter_view}{Callback applying a validated scatter view
(as used by the \code{set_scatter_view} tool).}

\item{state}{Builder function called as \code{state(sections)} returning
the compact assistant state (overview plus any requested full-detail
sections; mirrors the \code{get_omics_viewer_state} tool surface).}

\item{store}{Canonical widget store driving the S3 generic widget tier
(the exact store the \code{set_widgets} tool writes to).}

\item{apply_enrichment}{Optional callback applying a validated ORA/fGSEA
update (as used by \code{set_enrichment_parameters}).}

\item{apply_table_view}{Optional callback applying a validated table-view
update (as used by \code{set_table_view}).}

\item{assistant}{Optional WP11 assistant API (from
\code{\link{ai_assistant_module}}) exposing the conversation
snapshot/restore path used by the .ESS machinery.}
}
\value{
UI tags and a module server, plus the hook-enabled predicate.

TRUE when \code{OMICSVIEWER_TEST_HOOKS} is true, yes, on, or 1.
}
\description{
These hooks exist exclusively for headless UI testing (shinytest2 /
Playwright). They expose the exact \code{apply_agent_state} and
\code{apply_agent_scatter_view} callbacks used by the ellmer assistant
tools, so UI-effect tests drive the identical server-side pathway a model
tool call drives. The hooks are rendered and wired only when the
environment variable \code{OMICSVIEWER_TEST_HOOKS} is set to a true-like
value; ordinary deployments are unaffected. The hook UI is hidden from
users and never provides capabilities beyond the assistant tools
themselves.
}
\keyword{internal}
